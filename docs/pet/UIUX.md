# UIUX 文档 v1.0 · iuvim 桌宠挂件

> 生成日期：2026-08-29
> 状态：待用户确认（Phase 1 门禁）
> 基于：PRD v1.0 + 设计师 Phase 1 设计调研
> 风格关键词：**栖木感、克制灵动、温暖克制、玩家工艺、帧即生命**

---

## 1. 设计语言：双寄存器

| 寄存器 | 范围 | 对标 | 原则 |
|--------|------|------|------|
| Product 寄存器 | 工具栏本体、设置页 | Linear / Vercel | 扁平克制、细边框、输入法用户熟悉感；不喧宾夺主 |
| Brand 寄存器 | 宠物本体、工坊 UI | VPet + Bongo Cat | 状态驱动动画、独特性、陪伴感；宠物是情绪出口 |

工坊界面套 Wallpaper Engine 三 Tab 范式：**已安装 / 发现 / 工坊**，内置浏览订阅不跳出应用。

## 2. 配色 Token（锁定——全部 Token 引用，无硬编码）

### 2.1 深色皮肤（默认）

| Token | 色值 | 用途 |
|-------|------|------|
| `bg-base` | `#0E1113` | 炭底 |
| `bg-surface` | `#171A1D` | 表面（面板/窗口） |
| `text-primary` | `#EDEDEB` | 暖白字 |
| `accent` | `#F5A524` | 琥珀暖橙（唯一强调色，避开靛蓝 AI 病，暖色衬陪伴感） |
| `success` | `#34D399` | 语义绿 |
| `warning` | `#EAB308` | 语义黄 |
| `danger` | `#F87171` | 语义红 |

### 2.2 浅色皮肤（可选皮肤）

| Token | 色值 | 用途 |
|-------|------|------|
| `bg-base` | `#FAF9F7` | 暖白底 |
| `accent` | `#C2410C` | 深琥珀（浅底对比度达标） |

## 3. 字体方案

| 场景 | 字体 |
|------|------|
| 原生工具栏（tiny-skia 小字号保真） | 系统栈：Microsoft YaHei UI + Segoe UI Variable |
| 展示字（商店页/工坊头图） | Bricolage Grotesque + Noto Sans SC |
| 等宽（状态码 / mod 配置） | JetBrains Mono |

## 4. 宠物与工具栏融合范式

核心原则：**工具栏 = 控制信道，宠物 = 状态信道，互不抢焦**。

### 4.1 栖木式吸附
宠物趴工具栏上沿（边线作栖木），视觉上"栖息"而非"拼接"。

### 4.2 状态共生动画（四态联动）

默认宠已由 M1 像素狗升级为**二次元少女半身像**（详见 `GIRL-PET-SPEC.md`），
四态联动改为少女的表情 + 头发动作表现：

| 输入法状态 | 少女表现 |
|------------|----------|
| 中文模式 | 常态表情 |
| 英文模式 | 打盹表情（偷瞄） |
| 全/半角切换 | 惊讶表情一闪 + 头发甩动 |
| 简/繁切换 | 惊讶表情一闪 + 头发甩动 |
| 打字中 | 专注表情 |
| 点击互动 | 微笑 + 头发弹动 |
| 闲置 | 呼吸起伏 + 随机眨眼 |

> 表情素材按 6 张一组组织（`normal`/`blink`/`smile`/`focus`/`surprised`/`sleepy`），
> 当前 `smile`/`focus`/`surprised`/`sleepy` 暂用 `normal` 回退，代码路径已就绪。

### 4.3 回巢锚点
宠物闲逛（非 MVP，预留）后自动跳回工具栏位置。

### 4.4 碰撞规避
宠物动画不遮挡 6 按钮热区；点击穿透走 ULW alpha。

### 4.5 闲置态
VPet 式挂边窥视（间隔出现，非持续动画，省电）。

## 5. 图标系统（锁定）

- **图标库 = Lucide**（ISC 许可可商用）：24×24 统一 2px 描边 + currentColor，与扁平细边框同构。
- 尺寸规范：16px（行内）/ 20px（按钮内）/ 24px（独立图标），全项目一致。
- 实现：path data 嵌入 Rust/tiny-skia 绘制（约 20 枚）。
- **全项目禁止 emoji 作功能图标（P0）**；桌宠动画素材（精灵帧 PNG）不属图标范畴。

## 6. 界面清单（MVP）

| 界面 | 要点 |
|------|------|
| 工具栏 + 宠物复合窗口 | 栖木式；宠物居中挂工具栏正上方（不占右侧宽度）；深色默认皮肤；复合窗 212×178 |
| 宠物设置页（设置窗内） | 宠物开关、换宠（**本次不实现**）、mod 管理入口（**本次不实现**） |
| 工坊浏览（三 Tab） | 已安装/发现/工坊（**已转 Backlog，本次不实现**） |
| 默认宠物 | 二次元少女半身像：分层摆动 + 呼吸 + 眨眼 + 状态表情 |

## 7. 变更记录

| 日期 | 变更内容 | 原因 | 影响范围 |
|------|----------|------|----------|
| 2026-08-29 | v1.0 初版 | Phase 1 调研结论汇编 | — |
| 2026-08-29 | 默认宠改为二次元少女半身像（112×128）；宠物区 128 px；工坊浏览与换宠 UI 转 Backlog | 本期聚焦形象改造，工坊线待议 | §4.2、§6；详见 `GIRL-PET-SPEC.md` |
| 2026-08-30 | 宠物改挂**工具栏正上方居中**，取消右侧 128 px 宠物区（复合窗 340×178 → 212×178） | 旧布局复合窗比工具栏宽 128 px，工具栏拖不到屏幕右边缘 | §6；`GIRL-PET-SPEC.md` §2.2、`M1-IMPLEMENTATION.md` §5.1 |
